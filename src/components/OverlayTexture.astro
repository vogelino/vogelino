---
import classNames from "../utils/classNames";
---
<div
  style={{
    backgroundImage: `url(/images/textures/film-grain.gif)`,
    backgroundSize: "400px auto",
  }}
  aria-initial="true"
  class={classNames(
    `fixed inset-0 bg-cover opacity-5 z-50`,
    `pointer-events-none mix-blend-exclusion`,
  )}
/>

